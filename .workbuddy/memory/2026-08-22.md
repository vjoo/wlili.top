# 2026-08-22

## tools/school-calendar.html 优化
- 学期/假期时间判断修复：原 `renderCalendar` 用所有假期起点最小值作学期终点，会误把秋假/春假（学期中短假）当终点截断日历。改为只取终止性假期（T1=寒假、T2=暑假）起点作边界。
- 寒假/暑假起点改为 `endDate + 1`（学期结束次日开始放假），与“时间安排参考”文案一致；副标题显示真实最后上课日。
- 新增校验：终止性假期须晚于开学；短假须落在开学与学期结束之间。
- 移动端交互：桌面/打印保留 A4 表格；≤600px 隐藏表格，改为竖向“周卡片”列表（JS 同时生成两份 DOM，renderCalendar 重构）。
- 学年选择 UI：原 `<select>` 只有 2026 一个选项。→ 第一次误改成步进器+chips，**用户反馈改错地方（学年下拉本来是好的）**，已还原为原 `<select id="yearSelect">`。
- 「年份难选」的真相：用户指的是**表单里的日期输入框**（开学/报到/寒暑假起止/秋假/春假/自定义标注）用原生 `<input type="date">`，手机上是滚轮、年份要一直滑、很隐蔽。
- 修复方案：新增 `convertDateInput()` 把原生日期框替换为「年/月/日」三段 `<select>` 下拉（年份单独成块、点按即选），保留隐藏镜像 `<input id=原id class="... date-mirror">` 沿用 `.value`，既有读取/校验/校验报错逻辑零改动。
- 关键衔接：`updatePreset()` 程序化赋值时通过 `refreshAllDateFields()` 把隐藏 `.value` 同步回三段下拉；`displayEl()` 让报错高亮落到 `.date-field` 外层；空值保留（不默认填日期，避免误显假期）。
- 移动端：`.breaks-range` 在 ≤600px 改为起/至/止各占一行，避免 6 个下拉挤一行。
- 桌面端假期标题/日期同行的 bug：`.breaks-row` 原 `align-items:center; gap:16px`（标题与起/至/止同一行），年份下拉被挤成 "202..."。改为 `flex-direction:column; align-items:stretch`，标题独占一行、日期区间在下方整行，年份 "2026年" 完整显示。与移动端“上下两行”观感统一。
- “+添加标注”按钮从右侧移到左侧「自定义（生日、考试时间等）」标题之前。
- 时间安排参考新增「共 X 周」：新增 `countWeeks(reg,start,vacStart)`，算法与 `renderCalendar` 的 totalWeeks 完全一致（min(报到,开学) 的周一 → 放假起始日所在周周日，ceil(天数/7)），保证参考标题周数与日历副标题一致。T1=22周、T2=21周。
- 标签配色与字号：国家法定节假日（中秋/国庆/元旦/清明/劳动/端午，type:'holiday'）改橘色 `#ffa617`（深棕字 #3a2600 保证对比）；学校假期（寒暑假/春秋假）原也用 type:'holiday' 混在一起，改为 `expandDateRange(...,'break')` 独立染红 `#d32f2f`（新增 `.tag-break`）。无需改几百行预设数据。标签字号 10px→12px（桌面+移动周卡片统一），去掉 `transform:scale(0.9)`。
- A4 边距收紧：屏幕 `.a4-paper` padding 15mm→12mm；打印 `@media print` padding 8mm 10mm→6mm 8mm。估算 2026 T1（22周，约261mm 内容）在 273mm 内容区（12mm 边距）内可容纳，留约12mm 余量，不超 A4。
- 移动端日历改回与打印一致：用户反馈竖向周卡片「不好看、和打印差别太大、会误导人」。将 ≤600px 块的 `.cal-table{display:none}` 改为 `display:table`、`.a4-paper` 恢复 `210mm` 宽（可横向滑动）、隐藏 `#mobileCal`、显示 `.scroll-hint`。即移动端直接显示与打印/桌面一模一样的 A4 表格。原周卡片 CSS/JS（`#mobileCal`/mobile-week 等）现处于 dormant 状态（hidden），未删除以免误伤。
- 鸿蒙浏览器兼容（年份被截断「2…」）：① 年份 option 文字 `yy+'年'` → 纯数字 `yy`；② `.date-field` 在 ≤600px 改 `flex-wrap:wrap`，年份 select `.df-year{flex:1 1 100%}` 独占一行、月日各半行；③ `.breaks-range` 在 ≤600px 改 `flex-direction:column`（原 `flex-wrap+flex-basis:100%` 在鸿蒙不稳，导致起/止仍并排），起/至/止 竖向堆叠，每个日期框整行宽、年份再独占一行。彻底解决截断。
- 日历格子等高：`.cal-table td:not(.col-week)` 原 `height:38px` 但标签 baseline 对齐 + 多标签撑高导致行高不齐。改 `height:44px; vertical-align:middle; line-height:1.3; overflow:hidden`（保留 table-cell 默认行为，居中 + 防撑高）。⚠️ 注意：曾试过 `display:flex` on `td`（flex-direction:column 居中），但在鸿蒙/部分浏览器下 table-cell 改 flex 会导致表格渲染错乱（行高异常/内容错位），已回退，勿再用 flex on td。
- 第一周规则：**开学的周才记第 1 周**（用户明确要求）。报到若在开学前一周的周日，旧逻辑 min(报到,开学) 会把报到周当作第1周 → 错。改法：`firstTeachingMonday = getMonday(开学日)`，日历起点仍取 min(报到,开学) 的周一（保留报到显示），循环内按 `weekMonday < firstTeachingMonday` 标「报到」、否则按 `(weekMonday-firstTeachingMonday)/7+1` 编号；`totalWeeks`（副标题共X周）改为从 firstTeachingMonday 起算；`countWeeks()` 同步改以开学日为起点（去掉 min 逻辑）。`vacMonday = teachingLastSunday+1` 判假期周。
- 教学周边界修正（T2 bug）：暑假 2027-07-05 恰为周一，旧逻辑把该周算作「第20周」但整周无课。新规则：**放假起始日若为周一 → 教学周止于前一周日（该周整周无课不算教学周，直接进入假期行）；放假在周中开始（周二~周日）→ 该周仍算最后教学周**。renderCalendar 的 `endEnd` 与 `countWeeks` 同步此逻辑。T2=19周、T1=22周（寒假周六开始不受影响）。
- 假期标签精简：寒假/暑假跨数十天，逐日红字太乱。新增 `sparseBreakRange()` 只生成起止两天标签（「寒假开始/寒假结束」「暑假开始/暑假结束」），中间日期无标签；秋假/春假较短保持逐日标注（`expandDateRange`）。breakDefs 加 `sparse:true` 标志区分。格子无 break 底色类，中间日期显示为普通月交替底色。

## portfolio/cases 主题切换（浅色/深色，全站共享）
- 存储：cases.json 顶层 `theme`（'light'/'dark'，缺省 light）。server.py 加 `pf_load_theme`/`pf_save_theme` + GET/POST `/api/theme`（POST 需登录鉴权，同 navmenu 模式）。server.py 在 .gitignore（本地文件，改后必须重启服务才生效）。
- 后台 UI：admin.html 页面管理 topbar 左侧加 `.theme-switch` 分段控件（浅色/深色），`loadThemeState`/`applyThemeSwitch`/`bindThemeSwitch`（bind 放 init 通用位置，load 放页面管理分支）；点击 POST /api/theme + toast。
- 前台应用：site-shell.js `applyTheme()` 给 documentElement 加/去 `data-theme="dark"`；boot 前同步读 localStorage('case_theme') 防首屏闪烁，`loadNavMenu` fetch cases.json 时读 `d.theme` 权威覆盖并写回缓存。
- 深色 CSS 体系（site-shell.css 末尾）：
  - `[data-theme="dark"]` 反转变量：--color-background→#0a0a0a（与 Let's talk 一致）、--color-text-secondary→rgba(245,245,245,.72)、--color-black→#f5f5f5（文字/深填充反浅）、--color-white→#1a1a1a（白卡底→深卡）、--color-primary、--color-gray-ultra-light、color-scheme:dark。
  - **豁免区**（内部变量还原为浅色主题值 + `color: var(--color-black)` 防 body 继承浅字）：`.header-overlay`（顶部导航，⚠️ 不是 .site-header）、`.mobile-menu`、`.footer`（底部不参与变色）、`.hero-section`、`.services-section`、`.testimonials-section`、`.hero-banner-section`、`.swipe-slider-section`、`.fullscreen-slider-section`（本来就深底白字/图底白字）。必须还原 --color-background（移动菜单背景）！必须显式 color（header 文字继承 body 的 --color-text-secondary，仅还原变量不够）。
  - components.css 末尾硬编码覆盖：白卡（.project-card/.project-card-image/.client-logo/.stat-item/.team-card-wrap）→#1a1a1a；黑字（.stat-value/.stats-title/.stats-subtitle/.team-title-a）→#f5f5f5；灰字（.stats-label→#9a9a9a；.stat-desc/.team-title-b/.team-desc→rgba(245,245,245,.72)）；边框（.info-row→rgba(255,255,255,.14)）；.masonry-title/.masonry-tag 白字保持 #fff（var(--color-white) 反转成深色会坏）。
- 实测：bodyBg #0a0a0a、client-logo/stat-item/team-card/screen-content #1a1a1a、标题 #f5f5f5、导航胶囊滚动后仍白底、footer 白字白二维码卡不变。服务在本地由 `python -u server.py` 后台运行（8080），agent-browser 实测后已关闭。
- ⚠️ footer 区域几乎全硬编码（#fff/rgba(255,255,255,…)），不受变量影响；.qr-card 背景硬编码 #fff 保持白。.back-to-top-btn 用 var(--color-black/--color-white)，深色下自动变浅底深图标（可见）✓。
- ⚠️ 加载动画不参与变色（用户发现后修复）：① `.page-transition-overlay`（页面过渡黑遮罩）背景 var(--color-black) 深色下会变白遮罩 → `[data-theme="dark"] .page-transition-overlay { background:#0a0a0a }`；② `#page-skeleton`（首屏骨架屏）内 `.block`/`.logo-text`/`.subtitle-text` 用 var(--color-white) 深色下变深块看不见 → `[data-theme="dark"] #page-skeleton { --color-white:#fff; --color-black:#0a0a0a }` 变量还原。加载动画属品牌壳层（同 header/footer），一律保持黑遮罩+白 logo 原样。
- ⚠️ 深色主题跳转页面首帧白屏（用户发现后修复）：新页面 body 背景在 site-shell.js 加载前按浅色渲染（data-theme 尚未设置）。修复：**每个页面 index.html 的 head 顶部「第一优先级滚动修复」内联脚本（任何 CSS 之前）加主题应用**——`var _th=localStorage.getItem('case_theme'); if(_th==='dark') document.documentElement.setAttribute('data-theme','dark')`。已同步到 _template + 全部 5 个页面（home/pjlist/mybilist/vjooProject/reeoder），L17 先于 site-shell.css L63。⚠️ 以后手写/复制新页面时，此 head 脚本由模板自带，勿删；若主题逻辑有变需同步改所有页面 + 模板。

## 加载动画选择（加载动画1/加载动画2，全站共享）
- 存储：cases.json 顶层 `loader`（'1'=首屏骨架屏（默认）/ '2'=页面过渡黑遮罩）。server.py 加 `pf_load_loader`/`pf_save_loader` + GET/POST `/api/loader`（同 theme 模式，POST 需鉴权）。
- 前台：`data-loader` 属性控制显隐（CSS）：`:root[data-loader="1"] .page-transition-overlay, :root:not([data-loader]) .page-transition-overlay { display:none }`（骨架屏模式隐藏黑遮罩）；`:root[data-loader="2"] #page-skeleton { display:none !important }`（黑遮罩模式隐藏骨架屏）。site-shell.js `applyLoader()`（localStorage 缓存防首帧 + loadNavMenu fetch cases.json 权威覆盖）+ `currentLoader()`；initGridAnimation 里 **loader=2 时跳过 Three.js 马赛克幕布**（否则黑幕布盖住黑遮罩）。
- 后台：admin.html 页面管理 topbar 主题切换旁加 `#loaderSwitch`（加载动画1/2 分段控件）+ loadLoaderState/applyLoaderSwitch/bindLoaderSwitch（照 theme 三件套）。
- head 脚本（模板+5页面）同步加 `case_loader` 预应用（`var _ld=localStorage.getItem('case_loader'); if(_ld==='1'||_ld==='2') document.documentElement.setAttribute('data-loader',_ld)`），在 case_theme 片段后。
- ⚠️ 关键发现：**只有 home 页面的骨架屏有 3×3 方块 + WLI logo**，其他页面 sk-loader 是空 div（只有 sk-hero/sk-line 骨架条）→ initGridAnimation blocks=0 直接 return（马赛克幕布也不创建）→ 其他页实际显示的是黑遮罩。已把所有页面（_template/pjlist/mybilist/vjooProject/reeoder）的骨架屏统一为 home 结构（grid-animation 9×.block + logo-text WLI + subtitle-text）。加载动画1（骨架屏）= 黑马赛克幕布（THREE，uColor #000）+ 白 WLI logo + 3×3 白方块，颜色硬编码/豁免不随主题。
- ⚠️ 部分文件被系统锁定（Permission denied，python/git 写不了）：_template/index.html、cases.json、mybilist/index.html 等 → 用 Edit 工具可写（工具走特殊通道）。cases.json 还原测试字段时若 git checkout 报 unlink 错误，直接用 Edit 删字段。
- 实测：loader=1 → 骨架屏 9 方块+WLI logo 接管、遮罩隐藏、马赛克幕布创建；loader=2 → 骨架屏 CSS 隐藏、马赛克不创建、黑遮罩（rgb(10,10,10)）。后台切换保存 → cases.json → 前台权威读取 ✓。
