# 局域网同步服务器相关（不上传到 GitHub）
server.py
server_data.json

# 片源探测服务：本机运行的服务脚本，属「本地服务脚本」范畴，不进公开仓库
# （tools/source-probe.html 页面会上线，访问者需自备此脚本才能跑通）
tools/probe_server.py

# 工具页双通道数据文件（规则 29：用户私有数据不上云，由 server.py 运行时创建）
# ⚠️ 必须用通配，不要写精确文件名：曾经只写了 tools/prompt-library-data.json，
#    漏掉了 tools/prompt-library-data.json.bak-20260923（76MB）被提交入库，
#    触发 EdgeOne 单文件 25MB 上限导致部署失败、新工具上不了线（2026-09-24）
tools/*-data.json*

# 本地工具与运行时缓存
_restart_server.py
__pycache__/
*.pyc

# 本地 AI 配置
.trae/

# ⚠️ 安全红线：以下文件含本机绝对路径/本地运维信息，严禁提交（仓库公开时=泄露隐私路径）
# 本地运维脚本（启动/停止服务、防火墙规则、局域网同步）
*.bat
# Windows 系统缓存（含本机路径元数据）
Thumbs.db
# 一次性调试/修复脚本（含硬编码本机路径 D:\... 或 C:\Users\...），用完即弃，禁止入库
fix_*.py
backfill_*.py
_probe_*.js
_apply_*.js
scripts/check_redundant_files.py

# WorkBuddy 本地工具数据（工作日志/会话记忆，仅本地使用，不上传部署）
.workbuddy/
